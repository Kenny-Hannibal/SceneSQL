name: on_ramp
version: '1.0'
description: 上匝道：产线SQL直通模式(raw_sql)
blocks: []
variants:
  default:
    tag_name: on_ramp
    raw_sql: "select  start_ts,  end_ts, end_ts-start_ts duration,\n  'on_ramp' as tag_name\nfrom range_tag where tag_name='OnRamp' and end_ts-start_ts >4;"
